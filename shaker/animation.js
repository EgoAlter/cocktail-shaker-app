// All canvas animation for the shaker sequence. Pure visual output — no game logic.
// Each function is independently swappable when real assets arrive.

export function drawShaker(ctx, state) {
  // Stub — Phase 1D
}

export function drawIngredient(ctx, ingredient) {
  // Stub — Phase 1D
}

export function drawLiquid(ctx, fillLevel) {
  // Stub — Phase 1D
}

export function drawPour(ctx, progress) {
  // Stub — Phase 1D
}

export function drawGlass(ctx, fillLevel) {
  // Stub — Phase 1D
}

export function drawShakeEffect(ctx, intensity) {
  // Stub — Phase 1D
}
