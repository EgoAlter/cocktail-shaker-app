// In-shaker HUD — shake intensity meter, pour progress indicator.
// Stub — Phase 1D.

export const HUD = {
  drawShakeMeter(ctx, intensity) {
    // Stub
  },
  drawPourProgress(ctx, progress) {
    // Stub
  },
};
