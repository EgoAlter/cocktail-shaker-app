// Full-screen HTML overlays for WELCOME, QUESTIONING, RESULT, DONE states.
// HTML overlays sit on top of the canvas — easier text input than canvas drawing.
// Stub — wired up in Phase 1C.

export const Screens = {
  show(screenName) {
    // Stub
  },
  hide(screenName) {
    // Stub
  },
};
